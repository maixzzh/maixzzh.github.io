title: 张麦轩                    # 导航栏左侧用户名，首页姓名
email: maixuanzhang7@gmail.com       # 您的联系邮箱
description: 习惯把世界看成点和边：相遇是边，时间是边权，回忆是强连通分量；愿我们之间的图没有负环，只有一条通往彼此的最短路。         # 站点描述
signature: 把人生拓扑排序，找到通往 AC 的最短路              # 首页个性签名（可自定义）
lang: zh-CN                         # 语言
url: "https://maixzzh.github.io"   # 站点域名（务必替换为您的 GitHub Pages 地址）
baseurl: ""                         # 用户站点必须为空字符串
encoding: utf-8
theme: minima
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag
permalink: /:year/:month/:day/:title/
date_format: "%Y-%m-%d"
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
